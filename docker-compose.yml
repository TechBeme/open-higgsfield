services:
  open-higgsfield:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - open-higgsfield-data:/app/data
    restart: unless-stopped

volumes:
  open-higgsfield-data:
